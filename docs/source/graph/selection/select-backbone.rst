Select Backbone
```````````````

Add to the current selection set of all nodes and transactions whose nodes are connected to more than 1 transaction. Excludes transactions that has a node connected only to itself.


.. help-id: au.gov.asd.tac.constellation.graph.visual.selectBackbone
