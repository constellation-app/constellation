# Import/Export JDBC - Tables

The tables panel defines the tables that will be exported to or imported
from.

The list of tables is created by looking in the schema defined by the
JDBC connection URL.
